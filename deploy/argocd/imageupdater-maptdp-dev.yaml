apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: maptdp-dev
  namespace: argocd
spec:
  applicationRefs:
    - namePattern: "maptdp-dev"
      images:
        - alias: frontend
          imageName: docker.io/jaffleman971/maptdp-frontend
          commonUpdateSettings:
            updateStrategy: name
            allowTags: "regexp:^sha-[0-9a-f]+$"
            forceUpdate: true
          manifestTargets:
            helm:
              name: "frontend.image.repository"
              tag:  "frontend.image.tag"

        - alias: backend
          imageName: docker.io/jaffleman971/maptdp-backend
          commonUpdateSettings:
            updateStrategy: name
            allowTags: "regexp:^sha-[0-9a-f]+$"
            forceUpdate: true
          manifestTargets:
            helm:
              name: "backend.image.repository"
              tag:  "backend.image.tag"

  writeBackConfig:
    method: git
    gitConfig:
      branch: main
      writeBackTarget: "helmvalues:values-dev.yaml"